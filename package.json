{
  "name": "smartsupport",
  "private": true,
  "description": "SmartSupport: intelligent customer service triage & resolution hub (MERN + Gemini + Pinecone)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "ping": "npm run ping --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
