{
  "name": "smartsupport",
  "private": true,
  "description": "SmartSupport: intelligent customer service triage & resolution hub (React + Supabase + Cloudflare Pages)",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "install:all": "npm install --prefix client"
  }
}
